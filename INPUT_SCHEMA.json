{
  "title": "Google play reviews",
  "description": "This actor scrapes app reviews from Google Play.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "appId": {
      "title": "Application id",
      "type": "string",
      "description": "Application id on google play - in url address after 'id='",
      "prefill": "com.neverland.alreader",
      "editor": "textfield"
    },
    "limit": {
      "title": "Reviews limit",
      "type": "integer",
      "description": "Number of reviews to be scraped",
      "prefill": 100,
      "editor": "number"
    }
  },
  "required": ["appId", "limit"]
}
